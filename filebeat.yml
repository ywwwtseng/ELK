filebeat.inputs:
  - type: log
    paths:
      - /usr/src/app/http-requests.log
    fields:
      log_type: pino-http

output.logstash:
  hosts: ["logstash:5044"]

setup.kibana:
  host: "http://kibana:5601"